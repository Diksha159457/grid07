# Minimal runtime dependencies for deployment.
# The app runs in lightweight mode by default and upgrades automatically
# if optional semantic dependencies are installed separately.
python-dotenv>=1.0.0
